const customStyle = {
    singleValue: (provided) => ({
      ...provided,
      color: 'black',
    }),
    option: (provided) => ({
        ...provided,
        color: 'black',
    })
  }

export default customStyle;